apiVersion: v1.0
kind: Service
metadata:
  name: mi-app-service
spec:
  type: NodePort
  selector:
    app: mi-app
    version: blue  # <--- AQUÍ SE CAMBIA DE 'blue' A 'green' PARA CONMUTAR EL TRÁFICO
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5678